type Movie {
  id: String!
  name: String!
  score: Int!
}

type Query {
  movies: [Movie]! # null일 수 없음. 무조건 반환할 것
  movie(id: Int!): Movie
}

type Mutation {
  addMovie(name: String!, score: Int!): Movie!
  deleteMovie(id: Int!): Boolean!
}
